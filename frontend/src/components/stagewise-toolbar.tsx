'use client'

import { StagewiseToolbar } from "@stagewise/toolbar-next"

export function StagewiseToolbarClient() {
  return (
    <StagewiseToolbar 
      config={{
        plugins: []
      }}
    />
  )
} 