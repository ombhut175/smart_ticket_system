# Backend server API base URL (do NOT include trailing /api; the frontend adds it)
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8080

# Node environment (development, production, etc.)
NODE_ENV=development